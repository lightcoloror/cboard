# CBoard 上游修复复用决策

## 2026-07-26：同步媒体上传与遥测修复

- **意图：** 在保留图语家双向沟通、微信迁移和当前脏工作树全部成果的前提下，及时吸收 CBoard 官方已经验证的稳定性修复，减少 fork 与官方主线的无意义分叉。
- **决策：** 不对当前工作树执行 merge、rebase 或整分支 cherry-pick；机械适配上游 `cfbe2fa664c2e380f52cdfc28ee2494302e9c218`、`12dfd1550fd94c142cec7930ea225afad89d8e60` 和 `c3d869952ec0da5e0b26ee29ab3e0e1e12d620cb`。同步前上传 base64 板面说明媒体，永久无效媒体清空、瞬时失败保留并阻止错误同步；未跟踪板遥测与既有 onboarding 资格保持一致。
- **理由：** 当前 fork 同一文件中已经包含图语家的图卡声音、AI 和跨端适配，直接合并上游会扩大冲突并可能覆盖并行开发。按行为和测试逐项移植可以完整复用官方修复，同时保持图语家新增能力不变。
- **证据：** 新增板面说明媒体与遥测边界测试后，目标回归为 `2 suites / 63 tests`，CBoard 全量为 `200 suites / 1364 tests / 72 snapshots`；目标 ESLint 与 `git diff --check` 通过。隔离目录 `BUILD_PATH=build-codex-upstream-sync` 的 production build 成功并生成 service worker。构建仍报告动态依赖和 Web 主 bundle `1.62 MB gzip` 偏大，这是既有整站性能债务，本轮未将其误记为已解决。
- **生效范围：** CBoard Web、复用同一源码构建的 Cordova/Electron 壳中的板面媒体同步和同步遥测；不改变图语家患者表达、接收理解、分词、图文匹配、语音、微信分包或 API 数据协议。本轮未 merge、rebase、提交、推送、预览、上传、发布或部署，也未打开、聚焦、抬升或置顶任何窗口。
- **记录：** Codex（GPT-5.6），2026-07-26 10:14:24。

## 后续同步边界

- 每次同步先只读 fetch 和审阅官方提交，再按功能边界适配；不得用整树覆盖处理包含图语家功能的脏工作树。
- 上游新增测试必须随行为一起移植；只有目标回归、全量回归、静态检查和 production build 都通过，才记录为已复用。
- `cboard-ai-engine` 当前未出现在本机认证态 `gh repo list cboard-org`，`gh api` 返回 404，Git HTTPS 又因 Windows 凭据错误无法读取 refs；但公开 GitHub 页面和搜索结果仍显示该仓库、189 条提交与 `1.9.0` release。当前只能判定“本机认证通道无法核验”，不能判定仓库不存在；本地 fork继续保留，待认证通道恢复后再做只读 fetch 与提交差异审阅。

## 更新记录

### 2026-07-26 10:14:24 | Codex（GPT-5.6）

- 新建上游修复复用决策，记录机械适配、验证证据和后续同步边界。

### 2026-07-26 10:14:24 | Codex（GPT-5.6）更正

- 更正 `cboard-ai-engine` 状态：公开页面仍存在，本机认证 API/HTTPS 失败不足以证明仓库被删除。
